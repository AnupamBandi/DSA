Key takeaways:
1.Intervals Based Problems -> Greedy + Sorting
2. python : letters.find(k) finds first occurance of k in letters and letters.rfind(k) finds last occurance of k and similarily indexOf() and lastIndexOf() in js works in the same way
